# Image Asset Fixes - Progress Tracker

## Planned Steps (Approved)
- [x] 1. Create this TODO.md
- [x] 2. Fix HeroSection.tsx path casing
- [x] 3. Fix CoursesHeroSection.tsx extension
- [x] 4. Convert Header.tsx logo to Next Image
- [x] 5. Fix TeachersHeroSection.tsx (path + fallback) + add onError to CoursesGridSection.tsx (fallback via existing path; courses images exist & verified)
- [ ] 6. Test with `npm run dev` and verify in browser
- [ ] 7. Update TODO.md as complete
- [ ] 8. attempt_completion

**Status**: Edits complete. Dev server running on http://localhost:3001 (existing server on 3000). All paths fixed to match public/assets (lowercase, correct extensions/casing). Logo uses Next Image with priority. Teachers hero uses existing hero image as fallback. Courses/Teacher images verified exist.

